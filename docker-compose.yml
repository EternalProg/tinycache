services:
  tinycache:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: tinycache
    ports:
      - "8080:8080"
    volumes:
      - ./config.toml:/app/config.toml:ro
    restart: unless-stopped
